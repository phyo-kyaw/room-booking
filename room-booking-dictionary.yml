apiVersion: apps/v1
kind: Deployment
metadata:
  name: room-booking-dictionary-api
spec:
  replicas: 1
  selector:
    matchLabels:
      app: room-booking-dictionary-api
  template:
    metadata:
      labels:
        app: room-booking-dictionary-api
    spec:
      containers:
        - name: room-booking-dictionary-api
          image: pkdocker3383/room-booking-dictionary-api:v1
          imagePullPolicy: Always
          ports:
            - containerPort: 8086
          #env:
            #- name: PORT
              #value: "8080"
---
apiVersion: v1
kind: Service
metadata:
  name: room-booking-dictionary-api
  labels:
    app: room-booking-dictionary-api
spec:
  ports:
    - port: 80
      targetPort: 8086
  selector:
    app: room-booking-dictionary-api
