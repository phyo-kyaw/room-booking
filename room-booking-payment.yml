apiVersion: apps/v1
kind: Deployment
metadata:
  name: room-booking-payment-api
spec:
  replicas: 1
  selector:
    matchLabels:
      app: room-booking-payment-api
  template:
    metadata:
      labels:
        app: room-booking-payment-api
    spec:
      containers:
        - name: room-booking-payment-api
          image: pkdocker3383/room-booking-payment-api:v1
          imagePullPolicy: Always
          ports:
            - containerPort: 8084
          #env:
            #- name: PORT
              #value: "8080"
---
apiVersion: v1
kind: Service
metadata:
  name: room-booking-payment-api
  labels:
    app: room-booking-payment-api
spec:
  ports:
    - port: 80
      targetPort: 8084
  selector:
    app: room-booking-payment-api
